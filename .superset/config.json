{
  "setup": [
    "mkdir -p app .claude && cp -f \"$SUPERSET_ROOT_PATH/app/.env\" app/.env && cp -f \"$SUPERSET_ROOT_PATH/admin/.env\" admin/.env && cp -f \"$SUPERSET_ROOT_PATH/mobile/.env\" mobile/.env && rsync -a --delete \"$SUPERSET_ROOT_PATH/.claude/\" .claude/ && claude"
  ],
  "teardown": []
}
