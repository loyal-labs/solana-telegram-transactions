{
  "setup": [
    "mkdir -p app .claude && cp -f \"$SUPERSET_ROOT_PATH/app/.env\" app/.env && rsync -a --delete \"$SUPERSET_ROOT_PATH/.claude/\" .claude/ && bun i --cwd app && claude"
  ],
  "teardown": []
}