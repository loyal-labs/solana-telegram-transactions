{
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- . ../packages ../app/src/features/private-transfer-analytics",
  "installCommand": "rm -rf node_modules && cd .. && bun install --frozen-lockfile",
  "buildCommand": "bun run build"
}
